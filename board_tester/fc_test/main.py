"""Future ``fc-test`` process entry point.

Milestone 2 will add the single supported ``run --config`` command. Keeping the
entry point separate from orchestration prevents CLI concerns from entering the
central runner.
"""

