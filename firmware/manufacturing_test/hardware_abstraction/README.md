# Hardware abstraction

Small interfaces for GPIO, SPI, I2C, USB, timers/timebase, and storage-facing operations will isolate component behavior from STM32 HAL details. This is a practical test-firmware boundary, not a universal portability framework.

