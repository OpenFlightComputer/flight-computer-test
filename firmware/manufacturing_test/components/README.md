# Components

Each component package will expose a consistent lifecycle to the application registry and use hardware abstractions or device drivers for low-level access. Components do not own global test ordering, operator interaction, or report files.

